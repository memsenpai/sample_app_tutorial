<%= form_for micropost.liking.build, remote: true do |f| %>
  <div><%= hidden_field_tag :liker_id, @user.id %></div>
  <div><%= hidden_field_tag :post_id, micropost.id %></div>
  <%= f.submit t("like"), class: "like-button" %>
<% end %>
